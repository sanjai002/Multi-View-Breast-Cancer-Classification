"""RL training entry point.

Implementation deferred to Phase 2. Will orchestrate agent training over the
trajectory environment: replay collection, optimisation, target-network updates,
logging and checkpointing.

Do not implement until Phase 1 is approved.
"""
