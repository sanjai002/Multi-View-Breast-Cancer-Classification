"""Markov Decision Process definition.

Implementation deferred to Phase 2. Will formalise the MDP: the state
representation (Phase 1 patient/image feature vectors concatenated with
prediction probabilities and trajectory context), the discrete action space
(e.g. screening / follow-up decisions), the transition dynamics over
trajectories, and terminal conditions.

Do not implement until Phase 1 is approved.
"""
