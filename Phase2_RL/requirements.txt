# Phase 2 dependencies. CPU-only is fine — there is no GPU code in this pipeline.
#
# Install (CPU torch, ~200 MB instead of ~2.5 GB for the CUDA build):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Versions are floors, not pins. Tested on Python 3.10.12 with
# numpy 2.1.3, pandas 2.3.3, scikit-learn 1.7.2, torch 2.5.1.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.0

# Optional — only for plotting figures yourself; the pipeline does not import it.
# matplotlib>=3.7
