"""Central configuration for Phase 2 (Reinforcement Learning).

Implementation deferred to Phase 2. Will define the RL hyper-parameters: state
and action dimensions derived from the Phase 1 feature exports, discount factor,
learning rates, replay-buffer size, network architectures, exploration schedule,
and training / evaluation settings.

Do not implement until Phase 1 is approved. See README.md for the Phase 1
artefacts this phase consumes.
"""
