"""Policies and action-selection strategies.

Implementation deferred to Phase 2. Will define exploration/exploitation
policies (epsilon-greedy, greedy, softmax) and helpers to derive a deployable
screening policy from a trained value function.

Do not implement until Phase 1 is approved.
"""
