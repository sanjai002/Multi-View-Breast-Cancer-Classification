"""RL environment (Gym-style) wrapping the screening MDP.

Implementation deferred to Phase 2. Will expose ``reset`` / ``step`` over patient
trajectories built from the Phase 1 exports, returning states, rewards and
termination signals for the Q-learning / DQN family of agents.

Do not implement until Phase 1 is approved.
"""
